import styles from './ContactsList.module.css';

const ContactsList = () => {
    return (
        <div className={styles.wrapper}>
            <h1>Contacts List</h1>
        </div>
    )
}

export default ContactsList;